// @ts-check
import { defineConfig } from "astro/config";

// Mirror this URL in src/lib/siteConfig.ts, public/robots.txt, and public/sitemap.xml.
export default defineConfig({
  site: "https://thebiggchillicecream.netlify.app",
});
